<nav class="navbar navbar-expand-lg navtext nnav">
  <div class="container">
    <a class="navbar-brand navtext py-1 d-none d-lg-block d-xl-block" href="<%= all_tweets_path %>"><%= image_tag "rt1.png" %></a>
    <a class="navbar-brand navtext pl-4 d-block d-lg-none d-xl-none" href="<%= all_tweets_path %>"><%= image_tag "rtmini.png" %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
        <a class="navbar-brand navtext py-1" href="<%= root_path %>"><i class='fas fa-user-friends text-white px-2 fa-2x tooltip-test' title="Tweets que sigo" ></i></a>
        <button type="button" class="btn" data-toggle="modal" data-target="#exampleModal"><i class="fas fa-search text-white fa-2x mr-3 tooltip-test" title="Buscar"></i></button>

        <!-- Modal -->
        <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title text-primary" id="exampleModalLabel">Buscador</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
              <div class="modal-body">
                <%= form_tag(root_path, method: :get) do %>
                <div class="form-group p-3">

                  <%= text_field_tag(:search, params[:search], :class => "form-control d-inline float-center") %>

              </div>
              <div class="modal-footer">
                <%= link_to "Limpiar", root_path, :class => "btn btn-info float-right ml-2" %>
                <%= submit_tag("Buscar", :class => "btn btn-primary text-white float-right ml-2")%>
              </div>
              <% end %>
            </div>
            </div>
          </div>
        </div>


        <%= image_tag (current_user.photo_url), :class => "avatar-in-tweet rounded" %>
          <%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete, class: 'nav-link text-white mx-2' %>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Registro', new_user_registration_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Login', new_user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
